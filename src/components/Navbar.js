import React from 'react'
import { AppBar, Toolbar, Typography } from '@mui/material';

export default function Navbar() {
  return (
    <AppBar>
    <Toolbar>
      <Typography>yoga app</Typography>
    </Toolbar>
    </AppBar>
  )
}
